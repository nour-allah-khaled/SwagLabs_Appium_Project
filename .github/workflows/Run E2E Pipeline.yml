name: Appium E2E

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  executionType: ci
  appiumServerUrl: http://127.0.0.1:4723

jobs:
  E2E_Appium_Android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Appium
        run: |
          npm install -g appium wait-on
          appium driver install uiautomator2

      - name: Run E2E tests on Android Emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          disable-animations: false
          emulator-options: >-
            -no-window
            -no-audio
            -no-boot-anim
            -gpu swiftshader_indirect
        script: |
          # Start Appium in background and log output
          appium -a 0.0.0.0 -p 4723 --log-level error > appium.log 2>&1 &
          echo "Waiting for Appium to be ready..."
          # Wait for Appium server with timeout 60s
          wait-on --timeout 60000 http://127.0.0.1:4723/wd/hub/status
          # Wait for emulator
          adb wait-for-device
          adb devices
          # Run Maven tests
          mvn clean test -Dtest=E2E -DexecutionType=${{ env.executionType }} -DappiumServerUrl=${{ env.appiumServerUrl }}

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-reports
          path: Test-out/allure-results
